package analytics

import (
	"context"
	"server/types"
	"testing"
)

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetBillTotals(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      []types.Transaction
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetBillTotals(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBillTotals() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBillTotals() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetBillTotals() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}

func Test_postgresRepo_GetMonthlyIncome(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		accountID string
		timeRange string
		want      float64
		wantErr   bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var r postgresRepo
			got, gotErr := r.GetMonthlyIncome(context.Background(), tt.accountID, tt.timeRange)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetMonthlyIncome() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetMonthlyIncome() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetMonthlyIncome() = %v, want %v", got, tt.want)
			}
		})
	}
}
